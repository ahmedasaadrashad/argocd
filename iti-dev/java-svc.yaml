apiVersion: v1
kind: Service
metadata:
  name: java-service
  labels:
    app: my-app
spec:
  selector:
    app: my-app           # Matches the pod labels
  ports:
    - protocol: TCP
      port: 80            # Service port (inside cluster)
      targetPort: 8080    # Container port
